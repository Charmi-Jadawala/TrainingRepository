exports.area = function(length, breadth) {
    return (parseInt(length) * parseInt(breadth));
}

exports.perimeter = function(length, breadth) {
    return (2 * (parseInt(length) + parseInt(breadth)));
}